package dev.frilly.locket.dto.res;

/**
 * A record that holds the string that tells the version, used as a
 * Responding DTO.
 *
 * @param version the version
 */
public record VersionResponse(String version) {

}
